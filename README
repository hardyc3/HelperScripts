Use ssh-keygen to create an ssh key
Use ssh-copy-id <username>@<hostname> to copy the key to the host you want to sync files on
run python FolderSync.py <local dir to watch> <remote dir to copy files to> <user>@<remote ip address>
